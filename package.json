{
  "name": "reelflow",
  "version": "0.1.0",
  "private": true,
  "description": "ReelFlow — SaaS que convierte un video crudo + descripción en un Reel listo para Instagram (silencios recortados, subtítulos animados en español, multi-formato).",
  "license": "UNLICENSED",
  "workspaces": [
    "web",
    "worker",
    "remotion"
  ],
  "scripts": {
    "dev:web": "npm run dev -w web",
    "dev:worker": "npm run dev -w worker",
    "build:web": "npm run build -w web",
    "build:worker": "npm run build -w worker",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
